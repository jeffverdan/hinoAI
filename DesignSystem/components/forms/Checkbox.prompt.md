Checkbox (square) or radio-style (round) with gold fill when checked.

```jsx
<Checkbox label="Aceito os termos" checked={ok} onChange={e=>setOk(e.target.checked)} />
<Checkbox round name="voz" label="Quarteto" />
```
