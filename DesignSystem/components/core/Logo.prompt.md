The Hino.AI brand lockup — gold music-note roundel + "Hino.AI" wordmark. Use in app headers, auth screens, and marketing.

```jsx
<Logo size={44} />
<Logo variant="sanctuary" size={40} />   {/* on dark backgrounds */}
<Logo showWordmark={false} size={32} />  {/* mark only, e.g. compact nav */}
```

Use `variant="sanctuary"` on any `[data-theme="sanctuary"]` / dark surface.
