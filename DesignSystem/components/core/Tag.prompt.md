Selectable chip — biblical themes (fé, esperança, gratidão), emotional tone, filters.

```jsx
<Tag selected onClick={...}>Gratidão</Tag>
<Tag>Esperança</Tag>
<Tag selectable={false}>Salmos 96</Tag>   {/* static label */}
```

Use `selected` + `onClick` for multi-select theme pickers.
