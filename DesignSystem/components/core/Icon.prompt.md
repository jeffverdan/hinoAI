Inline SVG icon from Hino.AI's curated Lucide set; inherits `currentColor` and used inside buttons, inputs and the player.

```jsx
<Icon name="play" size={24} color="var(--accent)" />
<Icon name="heart" />
```

Names: music, play, pause, mic, download, share, heart, sparkles, book, check, x, chevronRight, chevronLeft, arrowRight, plus, wave, user, lock, mail, church, wand. `play` renders solid; the rest are 2px strokes. For the full set use `lucide-react` in production.
